/**
 * Classe principal.
 * A aplicação realiza a leitura de um arquivo contendo os arquivos para construção da árvore.
 */
public class App{
     /**
      * Método principal que executa a aplicação.
      * 
      * @param args Argumentos da linha de comando (não utilizados nesta aplicação).
      */
     public static void main(String[] args) {
          /* ReaderFile.Reader("src\\teste.txt"); */
          ReaderFile.openDir();
     }
} 